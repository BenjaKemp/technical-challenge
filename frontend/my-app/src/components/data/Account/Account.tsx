import { AccountData } from '../../Dashboard/types';

const Account: React.FC<AccountData>  = ({ investors, holdings, rates }): JSX.Element  => {

    return (
      <>
        <p>this is Account</p>
      </>
    )
  }

  export default Account;